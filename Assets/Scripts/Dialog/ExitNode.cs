using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// A special type of <see cref="DialogNode"></see> that causes the dialog bubble to close after viewing this message
/// </summary>
public class ExitNode : DialogNode
{
    // Doesnt do anything special except trigger a dialog close after this message
}
